# Tasks

- [x] Add a regression check for dashboard KPI SQL/date semantics.
- [x] Fix `Admin::resumenDocumentosOperados()` daily sales and purchases to use business dates and numeric zero defaults.
- [x] Fix JSON-breaking debug output and date-window bugs in `Reportes::ventasHoy()` / `comprasHoy()` if still present.
- [x] Run focused validation and keep changed lines under 400.
- [x] Prepare PR notes linked to the dashboard regression context.
